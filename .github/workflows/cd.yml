name: CD

on:
    push:
        branches:
            - deploy

jobs:
    deploy:
        name: Deploy to Production
        runs-on: ubuntu-latest
        steps:
            - run: curl "https://api.nsm.mosaiq.dev/deploy/${{ secrets.NSM_PROJECT }}/${{ secrets.NSM_KEY }}"
